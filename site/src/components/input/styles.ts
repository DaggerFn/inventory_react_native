import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    input:{
        width: "80%",
        height: 64,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: "#121214",
    },
})